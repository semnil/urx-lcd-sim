# セキュリティポリシー

> For the English version, see [SECURITY.md](SECURITY.md).

urx-lcd-sim は GitHub Pages が配信する静的な Web ページである。サーバーコンポーネント・
アカウント機構・テレメトリはいずれも持たず、ページ自身のファイルを読み込む以外の通信は行わない。
シミュレーター上の本体が保持する値はブラウザの `localStorage` の 1 つのキーに保存され、ページが
URL から読む入力は `?zoom=` だけである。

## サポート対象バージョン

サポート対象は <https://urx-lcd-sim.semnil.com> で公開中のバージョンのみ。修正はバックポートでは
なく新しいリリースとして提供する。

## 脆弱性の報告

GitHub の非公開報告を使うこと —
**[Security → Report a vulnerability](https://github.com/semnil/urx-lcd-sim/security/advisories/new)**。
セキュリティ上の問題を公開 issue に投稿しないこと。

記載してほしい情報:

- バージョン (シミュレーターの SETUP → Version 画面の APP Version に表示)、ブラウザと OS
- 攻撃者が何を得られるか、および再現手順

単独メンテナーのプロジェクトのため、対応はベストエフォート。初回返信の目安は 1 週間程度。
希望がない限り、アドバイザリに報告者としてクレジットする。

## 対象に含まれるもの

- 細工された URL、またはページが描画する文字列 (シミュレーター上の本体に入力した名前を含む) から
  到達できるページ内でのスクリプト実行
- `BindingTable` が割り当てていないパスへの書き込みが、ホストアプリケーションのデバイスリンクに
  届いてしまう問題 ([パラメータのアドレス指定](docs/ja/architecture.md#パラメータのアドレス指定) を参照)
- <https://urx-lcd-sim.semnil.com> が、`main` の検査済みコミットのビルド以外を配信してしまう問題
  (ドメインの乗っ取りによるものを含む)
- リリースのワークフロー: タグ、ドラフト Release、各ジョブが受け取る権限
- リポジトリ・CI・リリースに含まれる秘密情報の露出

## 対象に含まれないもの

- 実機ファームウェア・本体そのもの・ヤマハ製ソフトウェアの脆弱性 — ヤマハに報告すること
- 独自のデバイスリンクを `BridgeTransport` の後ろに置くホストアプリケーション — そのアプリケーションに
  報告すること
- 攻撃者が既にサイトのオリジン上でスクリプトを、またはユーザーのマシン上でコードを実行できることを
  前提とする指摘 (`localStorage` への書き込みを含む)
- GitHub Pages そのもの — GitHub に報告すること
- 影響を実証していない自動スキャナーの出力
