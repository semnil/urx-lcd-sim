# コントリビューションガイド

> For the English version, see [CONTRIBUTING.md](CONTRIBUTING.md).

urx-lcd-sim は単独メンテナーのプロジェクトだが、外部からの協力を歓迎する。特に手持ちの実機との
比較 — 画面はユーザーガイドの図から作っており、図は実機と食い違うことがある。一覧に何が並ぶか・
いくつあるか・大小文字は、図だけでは確定しない。

セキュリティ上の問題は issue ではなく [SECURITY.ja.md](SECURITY.ja.md) の手順で報告すること。

## 協力できること

- **手持ちの実機との画面の比較。** 機種と、本体の SETUP → Version 画面に出る Total Version を書くこと。
- **issue の登録。** バグ報告・機能要望のテンプレートを使うこと。
- **プルリクエスト。** 新しい画面や、ユーザーガイドと異なる振る舞いを入れる変更は、先に issue を
  立てること。コードを書く前に根拠を合意しておくほうが早い。

## セットアップ

使うツールとコマンドは README の「[動かす](README.ja.md#動かす)」にある。

シミュレーター上の本体の状態はブラウザに残る。工場出荷時の状態から始めるときは、画面の上の
[Reset the unit] を使う。

## 規約

- 識別子とコメントは英語。コメントは振る舞いのみを説明する (経緯は書かない)。
- diff は最小限に、周囲のスタイルに合わせる。フォーマットのみの変更は単独では受け付けない。
- **ランタイムの外部依存はゼロ** — 配信するページに npm パッケージや CDN を持ち込まない。開発依存は可。

## 描く範囲

- **480x272 の画面の内側だけ。** 筐体・物理ノブ・端子・製品色・製品写真から採った色を持ち込まない。
- チャンネル/バスのアイコンは、チャンネルカラーの単色の四角で代える
  ([チャンネル画面のツールバー](docs/ja/screen-inventory.md#チャンネル画面のツールバー))。
  USER DEFINED KNOBS のつまみの絵は描く。
- 作らないコントロールは、実機と同じ位置に押しても何もしないボタンとして描き、決めた時点で
  [known-issues.md](docs/ja/known-issues.md) に書く。
- 実機がノブで回す値は、画面上のコントロールで届くようにする。`src/app/knob-reach.test.ts` がこれを固定している。

## 一次資料としてのユーザーガイド

- `reference/` に抽出したキャプチャはヤマハの著作物で、`.gitignore` 済み。コミット・ビルドに入れず、
  issue やプルリクエストにも添付しない — 図は出典で示す。再生成の手順は README の「画面の見た目の根拠」にある。
- 図から値を採る方法は [design-tokens.md](docs/ja/design-tokens.md#採取方法) に従い、ガイドの紙面に
  写っている範囲からだけ採る。
- 形・寸法・色は図から測ってよい。画面に何が並ぶか・いくつあるか・大小文字は、画面ごとに実機で確かめる。

## 実機との接続

画面は意味のドットパスだけで値を読み書きする。`BindingTable` は空で出荷し、接続した実機で検証して
いないアドレスを足さない ([パラメータのアドレス指定](docs/ja/architecture.md#パラメータのアドレス指定))。
`src/device/bridge-transport.test.ts` がこれを固定している。

## ドキュメント

- `docs/en/` と `docs/ja/`、`README.md` と `README.ja.md` はそれぞれ対を保つ。片方を直したら、同じ変更で
  他方の同じ節を直す。
- 図は Mermaid 記法で書く。触った図は描画して確かめてから出す。

## コミットとプルリクエスト

- コミットの件名は英語の命令形の文で、意味単位で分割する。
- プルリクエストのタイトルには種別の接頭辞 — `bug:`・`feature:`・`perf:`・`docs:`・`chore:`・
  `release:` — を付け、タイトルと本文は英語で書く。
- シミュレーターの変更は `CHANGES.md` と `CHANGES_ja.md` の未リリースの節に書く。
- 画面に出る変更には、同じ機種・表示倍率・切り出し範囲で撮った変更前後の画像を添える。
- GitHub Pages のワークフローは、`main` へのすべてのプルリクエストで `pnpm test` と `pnpm build` を実行する。
- リリースは `package.json` の `version` だけを変更するプルリクエストで行う。README の「ホスティング」を参照。

コントリビュートすることで、その成果物が [MIT ライセンス](LICENSE)の下で提供されることに同意したものとする。
